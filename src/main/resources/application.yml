spring:
  security:
    user:
      name: admin
      password: admin
  redis:
    timeout: 20s
    host: localhost
    port: 6379
    database: 3
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        max-wait: 20s
        min-idle: 5
        shutdown-timeout: 5000ms